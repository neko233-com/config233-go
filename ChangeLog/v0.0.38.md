# v0.0.38 (2026-03-20)

## ✨ 新增

### 泛型计数 API
- 新增 `config233.GetConfigListCount[T]()`
- 直接返回缓存中的配置数量，避免外部重复写 `len(config233.GetConfigList[T]())`

## 🐛 修复

### 泛型缓存读取更安全
- `GetConfigList` / `GetConfigMap` 改为安全读取缓存
- 避免零值或未初始化场景下的类型断言崩溃
- `GetConfigListCount` 不会触发结构体转换或生命周期回调

## 📚 文档

- 更新 `README.md`
- 更新 `QUICK_START.md`
- 更新 `pkg/config233/doc.go`

## ✅ 测试

- 新增计数 API 覆盖测试
- 通过 `go test ./...`

